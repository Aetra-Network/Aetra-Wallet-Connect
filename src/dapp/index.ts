/**
 * dApp side. `AetraConnect` is the one class a website uses: pair, await
 * approval, then request transactions and signatures.
 */
export { AetraConnect } from "./connector.js";
export type { AetraConnectOptions, ConnectHandshake } from "./connector.js";
